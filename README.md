# **PROJECT_NAME**

Generated with `new`.

## Install

```bash
bun install
```

## Commands

```bash
bun run dev
bun run lint
bun run format
bun run typecheck
```

Testing is intentionally opt-in for this scaffold. Configure it with the `setup-tests` skill.

## Environment

Set `APP_NAME` to override the default greeting name.
